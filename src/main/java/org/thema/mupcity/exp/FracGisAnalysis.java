package org.thema.mupcity.exp;

public class FracGisAnalysis {
/*	
	// pour utiliser cette méthode il faudra d'abbord automatiser la 
	//création d'un shp contenant les batis et les cellules véctorisées. 
	 * cette classe (ou je ne sais quoi) utilise des librairies obsolètes et fait donc planter les autres programmes
	public static double fracgisCorrelationVect (File shapefile) {
		 CorrelationMethod correlation = new CorrelationMethod(shapefile.getName(), 
             new DefaultSampling(), 
             new DefaultFeatureCoverage(DefaultFeature.loadFeatures(shapefile, true)));
     correlation.execute(new TaskMonitor.EmptyMonitor(), true);
     Estimation estim = new EstimationFactory(correlation).getDefaultEstimation();
     double dim = estim.getDimension();
     return dim;
}*/
}